version: '3.8'
services:
  db:
    image: postgres:16
    container_name: usp_found_db
    restart: always
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=poli_usp_2026
      - POSTGRES_DB=usperdidos
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - ./init_db:/docker-entrypoint-initdb.d # Scripts SQL iniciais
